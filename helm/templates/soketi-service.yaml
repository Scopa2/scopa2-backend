apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-soketi-service
spec:
  type: ClusterIP
  ports:
    - port: 6001
      targetPort: ws
      protocol: TCP
      name: ws
  selector:
    app: {{ .Release.Name }}-soketi
